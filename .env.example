# Base URL for your app or API
BASE_URL=https://example.org

# Default environment name (can be used to pick env files)
NODE_ENV=local

# Authentication (example)
AUTH_USERNAME=user@example.org
AUTH_PASSWORD=super-secret

# API keys / tokens (examples)
API_TOKEN=
CLIENT_ID=
CLIENT_SECRET=

# Test behavior toggles
HEADED=false
VIDEO=on
TRACE=on-first-retry
